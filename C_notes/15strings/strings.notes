! strings !

A string is a 1-D character array terminated by a null character('\0').
char s[] = {'Y','A','S','H','\0'};    //or
char s = "yash";

In memory, the characters of the string are stored in continous blocks each seperated by memory address of 1 byte.

    Y     A      S      H     \0
  1004   1005   1006   1007  1008
