//declaring strings using pointers.
#include <stdio.h>

int main(){
    char *ptr= "yash";  //this tells the compiler to store the string in memory and assigned address is stored in a char pointer.
    printf("%s",ptr);

    return 0;
}